    <optgroup label="<%= locals[:realms].first[:locale] %> Realms">
      <% locals[:realms].each do |realm| %>
        <%= tag :option, :value => realm[:slug], :content => realm[:name] %>
      <% end %>
    </optgroup>